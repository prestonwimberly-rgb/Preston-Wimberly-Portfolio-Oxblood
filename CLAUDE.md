# Preston Wimberly — Creative Portfolio

Agent guidance for this repo lives in `AGENTS.md`, shared with Codex. Read it first.
Use `README.md` for commands, `docs/design-direction.md` for the Prairie Airframe
design standard, and `docs/launch-checklist.md` for evidence and release gates.
Implementation details come from `package.json`, `netlify.toml`, and `config/`.

Read [.codex/SYNC-STATUS.md](.codex/SYNC-STATUS.md) for the current shared handoff.
Keep shared decisions in those tracked files so either agent can continue from
the same repository and branch. This import does not synchronize private chats
or verify that another agent has loaded the instructions.

@AGENTS.md
